const navbar = `
  <nav class="fixed top-0 left-0 w-full p-32 z-20">
    <img src="/logo/oudom_logo.svg" alt="Oudom Logo" width="48" height="48" class="invert">
  </nav>
`;

document.body.insertAdjacentHTML('afterbegin', navbar);
